#include <stdio.h>
#include<string.h>
void main()
{
    char ch[20][30];
    int i,un,nc=0;
    printf("enter any string");
    for(i=0;i<10;i++)
    puts(ch[i]);
    i=0;
    while(ch[i]!='\0')
    {
        if(ch[i]=='')
        {
            c++;
            i++;
        }
        i++;
    }
    printf("%d",c);i=0;
    if(ch[i]>=65)&&(ch[i]<90)|| ||ch[i]>=96&&ch[i]<=122
    {
        nc++;
        i++;
    }
    printf("%d",nc);
}
